package com.vajraworld.defender.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Light Theme Foundations
val BgLight = Color(0xFFF8FAFC)          // Slate 50 - pristine modern background
val SurfaceWhite = Color(0xFFFFFFFF)     // Pure White card surfaces
val SurfaceSecondary = Color(0xFFF1F5F9) // Slate 100 subtle container
val SurfaceElevated = Color(0xFFFFFFFF)  // Pure White elevated
val BorderLight = Color(0xFFE2E8F0)      // Slate 200 crisp hairline border
val BorderSubtle = Color(0xFFEEF2F6)     // Soft separator
val DividerColor = Color(0xFFE2E8F0)

// High-Contrast Professional Typography
val TextPrimary = Color(0xFF0F172A)      // Slate 900 rich obsidian text
val TextSecondary = Color(0xFF475569)    // Slate 600 clean subtitle
val TextMuted = Color(0xFF94A3B8)        // Slate 400 helper text
val TextWhite = Color(0xFFFFFFFF)

// Primary Brand & Tech Accents
val BrandBlue = Color(0xFF1E40AF)        // Royal Sapphire 700
val BrandBlueLight = Color(0xFFEFF6FF)   // Soft Sapphire tint (Blue 50)
val BrandBlueHover = Color(0xFF1D4ED8)
val AccentCyan = Color(0xFF0284C7)       // Electric Tech Cyan 600
val AccentCyanLight = Color(0xFFE0F2FE)
val PurpleAccent = Color(0xFF6366F1)     // Indigo 500
val PurpleAccentLight = Color(0xFFEEF2FF)

// Semantic Cyber Threat States
val SafeGreen = Color(0xFF059669)        // Emerald 600
val SafeGreenBg = Color(0xFFECFDF5)      // Emerald 50
val SafeGreenBorder = Color(0xFFA7F3D0)  // Emerald 200

val WarningAmber = Color(0xFFD97706)     // Amber 600
val WarningAmberBg = Color(0xFFFFFBEB)   // Amber 50
val WarningAmberBorder = Color(0xFFFDE68A) // Amber 200

val ThreatRed = Color(0xFFDC2626)        // Ruby 600
val ThreatRedBg = Color(0xFFFEF2F2)      // Ruby 50
val ThreatRedBorder = Color(0xFFFECACA)  // Ruby 200

// Legacy aliases mapped directly to clean light theme
val BgDark = BgLight
val CardDark = SurfaceWhite
val BorderDark = BorderLight
